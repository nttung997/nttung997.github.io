# Hex Decoder

A simple web tool to decode hexadecimal strings into readable text or URLs.

## Usage

1. Open `index.html` in a browser.
2. Paste a hex string (with or without spaces).
3. Click **DECODE** to see the result.
